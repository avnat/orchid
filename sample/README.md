# Sample folder

This folder exists to exercise Orchid's preview. Open the `orchid/sample`
directory in the app and click around.

- `architecture.md` — headings, code, tables, task lists, callouts
- `specs/data-model.md` — a nested file to test the tree

Nested folders are preserved; non-markdown files are hidden.
